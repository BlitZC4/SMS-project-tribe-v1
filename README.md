# SMS-project-tribe-v1
This repository contains code for module-5 Java, Student Management System using core Java that demonstrates fundamental concepts including object-oriented programming, JVM architecture understanding, inheritance, polymorphism, and basic backend principles.


# Student Management System (Java Console)

Simple console-based Student Management System built with core Java to demonstrate OOP concepts (encapsulation, inheritance, polymorphism, abstraction) and JVM understanding.

## Features
- Add, view, update, delete students
- Search students by ID or name
- Add courses to a student and list enrollments
- Menu-driven console UI using `ArrayList` for in‑memory storage

## Environment Setup
1. Install JDK 21 or later.
2. Set `JAVA_HOME` and update `PATH` so `java` and `javac` work from terminal.
3. Clone the repo:
